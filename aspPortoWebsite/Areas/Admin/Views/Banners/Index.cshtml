@model IList<Banner>


@if (TempData["Success"] != null)
{
    <div class="alert alert-primary" role="alert">
        @TempData["Success"]
    </div>
}
<div style="margin-bottom:50px">
    <a href="/Admin/Banners/Create" class="btn btn-outline-success" style="width:100%">Add new slider</a>
</div>

<table class="table table-bordered">
    <thead>
        <tr>
            <th scope="col">ID</th>
            <th scope="col">Image</th>
            <th scope="col">Title</th>
            <th scope="col">Description</th>
            <th scope="col">Button Text</th>
            <th>CRUD</th>
        </tr>
    </thead>

    <tbody>

        @foreach (var item in Model)
        {
            <tr>
                <th scope="row">@item.Id</th>
                <td>
                    <img width="200" height="150" src="~/images/banners/@item.Image" alt="Alternate Text" />
                </td>
                <td>@item.Title</td>
                <td>@Html.Raw(item.Description)</td>
                <td>@Html.Raw(item.ButtonText)</td>

                <td>
                    <a href="/Admin/Banners/Edit/@item.Id" style="color:red"><i class="fas fa-edit"></i></a>
                    <a href="/Admin/Banners/Delete/@item.Id" onclick="return confirm('Are you sure')"><i class="fas fa-trash-alt"></i></a>
                    <br />
                    <a href="/Admin/Banners/Details/@item.Id" style="color:red">Etrafli</a>
                </td>
            </tr>

        }

    </tbody>
</table>